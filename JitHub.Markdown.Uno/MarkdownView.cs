namespace JitHub.Markdown.Uno;

// NOTE: This file originally provided a code-only MarkdownView placeholder.
// The real control is now the XAML-backed MarkdownView in `Xaml/MarkdownView.xaml(.cs)`.
// We keep a (non-conflicting) legacy type name to avoid breaking any external references
// to this source file path, while ensuring compilation remains unambiguous.
internal sealed class MarkdownViewLegacyHost : SkiaMarkdownView
{
}
