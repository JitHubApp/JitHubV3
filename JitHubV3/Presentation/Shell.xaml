<UserControl x:Class="JitHubV3.Presentation.Shell"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:utu="using:Uno.Toolkit.UI"
      mc:Ignorable="d"
      d:DesignHeight="300"
      d:DesignWidth="400">
  <Border Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid x:Name="RootGrid"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch">

      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
      </Grid.RowDefinitions>

      <!-- Narrow: top compact status bar -->
      <Border x:Name="TopStatusBar"
              Grid.Row="0"
              AutomationProperties.AutomationId="ShellTopStatusBar"
              Padding="{StaticResource ShellStatusBarPadding}"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              Visibility="Collapsed">
        <Grid>
          <ItemsRepeater ItemsSource="{Binding StatusBar.Segments}"
                         Visibility="{Binding StatusBar.HasSegments}">
            <ItemsRepeater.Layout>
              <StackLayout Orientation="Horizontal" />
            </ItemsRepeater.Layout>
            <ItemsRepeater.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Text}"
                           Margin="0,0,12,0"
                           TextWrapping="NoWrap"
                           TextTrimming="CharacterEllipsis" />
              </DataTemplate>
            </ItemsRepeater.ItemTemplate>
          </ItemsRepeater>

          <TextBlock Text="{Binding StatusBar.Message}"
                     Visibility="{Binding StatusBar.ShowMessageFallback}"
                     TextWrapping="NoWrap"
                     TextTrimming="CharacterEllipsis" />
        </Grid>
      </Border>

      <utu:ExtendedSplashScreen x:Name="Splash"
                Grid.Row="1"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch">
        <utu:ExtendedSplashScreen.LoadingContentTemplate>
          <DataTemplate>
            <Grid>
              <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition />
              </Grid.RowDefinitions>

              <ProgressRing IsActive="True"
                    Grid.Row="1"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Height="100"
                    Width="100" />
            </Grid>
          </DataTemplate>
        </utu:ExtendedSplashScreen.LoadingContentTemplate>
      </utu:ExtendedSplashScreen>

      <!-- Wide: bottom status bar -->
      <Border x:Name="BottomStatusBar"
              Grid.Row="2"
              AutomationProperties.AutomationId="ShellBottomStatusBar"
              Padding="{StaticResource ShellStatusBarPadding}"
              Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
              Visibility="Collapsed">
        <Grid>
          <ItemsRepeater ItemsSource="{Binding StatusBar.Segments}"
                         Visibility="{Binding StatusBar.HasSegments}">
            <ItemsRepeater.Layout>
              <StackLayout Orientation="Horizontal" />
            </ItemsRepeater.Layout>
            <ItemsRepeater.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Text}"
                           Margin="0,0,12,0"
                           TextWrapping="NoWrap"
                           TextTrimming="CharacterEllipsis" />
              </DataTemplate>
            </ItemsRepeater.ItemTemplate>
          </ItemsRepeater>

          <TextBlock Text="{Binding StatusBar.Message}"
                     Visibility="{Binding StatusBar.ShowMessageFallback}"
                     TextWrapping="NoWrap"
                     TextTrimming="CharacterEllipsis" />
        </Grid>
      </Border>
    </Grid>
  </Border>
</UserControl>
