<Page x:Class="JitHubV3.Presentation.DashboardLayoutTestPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
      xmlns:layouts="using:JitHub.Dashboard.Layouts"
      xmlns:local="using:JitHubV3.Presentation"
      mc:Ignorable="d"
      IsTabStop="False"
      NavigationCacheMode="Required"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

  <Page.Resources>
    <ResourceDictionary>
      <local:StringNullOrWhiteSpaceToVisibilityConverter x:Key="StringNullOrWhiteSpaceToVisibilityConverter" />
    </ResourceDictionary>
  </Page.Resources>

  <Grid RowDefinitions="Auto,Auto,*"
        Padding="{StaticResource DashboardEdgePadding}"
        RowSpacing="{StaticResource DashboardSpacingM}">

    <utu:NavigationBar Content="{Binding Title}" />

    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="{StaticResource DashboardSpacingM}">
      <Button Content="Back"
              Command="{Binding GoBack}"
              HorizontalAlignment="Left" />

      <ComboBox Header="Layout Mode"
                MinWidth="220"
                ItemsSource="{Binding Modes}"
                SelectedItem="{Binding SelectedMode, Mode=TwoWay}" />
    </StackPanel>

    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
      <StackPanel Spacing="{StaticResource DashboardSpacingL}" MaxWidth="960">

        <TextBlock Text="Resize the window (Auto) or switch mode to exercise CardDeckLayout." Opacity="0.72" TextWrapping="Wrap" />

        <muxc:ItemsRepeater ItemsSource="{Binding Cards}">
          <muxc:ItemsRepeater.Layout>
            <layouts:CardDeckLayout
              Mode="{Binding SelectedMode}"
              ModeWidthThreshold="{StaticResource DashboardGridToDeckMinWidth}"
              CardMinWidth="320"
              CardHeight="180"
              Spacing="{StaticResource DashboardSpacingL}"
              MaxColumns="3"
              DeckMaxVisibleCount="5"
              DeckOffsetY="{StaticResource DashboardSpacingM}"
              DeckAngleStepDegrees="1.25"
              DeckScaleStep="0" />
          </muxc:ItemsRepeater.Layout>

          <muxc:ItemsRepeater.ItemTemplate>
            <DataTemplate>
              <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      BorderThickness="1"
                      CornerRadius="{StaticResource DashboardRadiusM}"
                      Padding="{StaticResource DashboardEdgePadding}">
                <StackPanel Spacing="{StaticResource DashboardSpacingS}">
                  <TextBlock Text="{Binding Title}" TextWrapping="Wrap" />

                  <TextBlock Text="{Binding Subtitle}"
                             Visibility="{Binding Subtitle, Converter={StaticResource StringNullOrWhiteSpaceToVisibilityConverter}}"
                             Opacity="0.72"
                             TextWrapping="Wrap" />

                  <TextBlock Text="{Binding Summary}"
                             Visibility="{Binding Summary, Converter={StaticResource StringNullOrWhiteSpaceToVisibilityConverter}}"
                             Opacity="0.72"
                             TextWrapping="Wrap" />
                </StackPanel>
              </Border>
            </DataTemplate>
          </muxc:ItemsRepeater.ItemTemplate>
        </muxc:ItemsRepeater>

      </StackPanel>
    </ScrollViewer>

  </Grid>
</Page>
